{
  "name": "view-memory",
  "version": "1.2.2",
  "description": "记住文件列表展开状态、笔记与 PDF 的阅读位置、画布视窗，重启后回到原处.",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
    "lint": "eslint src"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "^0.23.0",
    "eslint": "^9.39.5",
    "eslint-plugin-obsidianmd": "^0.4.2",
    "obsidian": "^1.7.0",
    "typescript": "^5.4.0",
    "typescript-eslint": "^8.70.0"
  }
}
